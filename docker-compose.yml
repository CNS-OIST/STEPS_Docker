version: '2.2'
services:
  steps:
    image: cnsoist/steps:3.1
    build: recipe
    hostname: $HOST
    ports:
    - "8888:8888"
    environment:
    - USER_LOGIN=$USER
    - USER_ID=$DUID
    - GROUP_ID=$DGID
    volumes:
    - $PWD/notebooks:/opt/src/notebooks
